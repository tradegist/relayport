---
name: export-new-model-to-types
description: Register a Pydantic model in SCHEMA_MODELS so it's exported to the TypeScript and Python type packages. Use when the user adds a new Pydantic model that consumers should be able to import (webhook payloads, API response types, shared primitives), or asks "how do I expose this type to TypeScript".
---

# Exporting a new Pydantic model to the type packages

The type packages (`@tradegist/relayport-types` for TS, `relayport-types` for Python) are generated from Pydantic models declared in `services/`. To expose a new model:

## The one required step

Add the model to `SCHEMA_MODELS` in `schema_gen.py`, under the correct key.

```python
SCHEMA_MODELS = {
    "shared": [
        Fill,
        Trade,
        OptionContract,
        # NewPrimitiveModel,  # ← add here for CommonFill primitives
    ],
    "relay_core.relay_models": [
        WebhookPayloadTrades,
        RunPollResponse,
        HealthResponse,
        # NewWebhookPayload,  # ← add here for relay API / notifier contracts
    ],
    "market_data.models.dividends": [
        DividendsUpcomingItem,
        DividendsUpcomingResponse,
        # NewMarketDataModel,  # ← add here for market data API
    ],
}
```

Then run:

```bash
make types
```

This regenerates:
- `types/typescript/<ns>/types.d.ts` (via `schema_gen.py`)
- `types/typescript/<ns>/index.d.ts` (via `gen_ts_barrels.py`)
- `types/python/relayport_types/<file>.py` (via `gen_python_types.py`)
- `types/python/relayport_types/__init__.py` (auto-AST-walked barrel)

## Choosing the right key

| Key | Maps to TypeScript namespace | Use for |
| --- | --- | --- |
| `"shared"` | `BrokerRelay` | CommonFill primitives (`Fill`, `Trade`, enums, `OptionContract`). Source: `services/shared/models.py` |
| `"relay_core.relay_models"` | `RelayApi` | Notifier payload contracts + relay API responses. Source: `services/relay_core/relay_models.py` (which re-exports from `relay_core/notifier/models.py`) |
| `"market_data.models.dividends"` | `MarketDataApi` | Market data endpoint types. Source: `services/market_data/models/dividends.py` |

If your model conceptually belongs to one of these layers but lives in a different file, **move the model to the right file** instead of registering a new key. Adding new top-level keys requires updating `gen_python_types.py` and `gen_ts_barrels.py` and is rarely correct.

## Supported entry types

`SCHEMA_MODELS` values may be:

- **`BaseModel` subclasses** — generated as `interface Foo` in TS, `class Foo(BaseModel)` in Python.
- **Discriminated-union `TypeAlias` values** — `WebhookPayload = WebhookPayloadTrades | WebhookPayloadOrders` style. Pydantic's `TypeAdapter` handles both via JSON Schema.
- **Class aliases** — `WebhookPayload = WebhookPayloadTrades` produces `export type WebhookPayload = WebhookPayloadTrades` in TypeScript via an `allOf: [$ref]` schema entry.

## Pydantic config that propagates to the generated types

- **`ConfigDict(extra="forbid")`** → `additionalProperties: false` in JSON Schema → strict TS types (no `[k: string]: unknown`). **Always use this on external-contract models** (webhook payloads, API responses).
- **No defaults on always-populated fields** — a default makes the field optional in JSON Schema → optional in TS (`fillCount?: number`). Defaults only for fields that are legitimately absent.
- **`Field(default_factory=list)`** for mutable defaults, only when the field is genuinely optional. Never bare `[]` or `{}`.

## Things that are NOT required

- ❌ No need to edit `types/typescript/<ns>/index.d.ts` — auto-generated by `gen_ts_barrels.py`.
- ❌ No need to edit `types/python/relayport_types/__init__.py` — auto-generated by AST walk.
- ❌ No need to edit `types/typescript/index.d.ts` unless you're adding a brand-new namespace (rare).

## Things that ARE manual

- ✅ The top-level barrel `types/typescript/index.d.ts` is **hand-maintained**. It declares the three namespaces (`BrokerRelay`, `RelayApi`, `MarketDataApi`). Add a new namespace here only if you genuinely need a new top-level grouping.
- ✅ Adding a new key to `SCHEMA_MODELS` requires also updating `gen_python_types.py` and `gen_ts_barrels.py` to know about the new source file. **This is rare.**

## Verification

After `make types`:

1. `git diff types/typescript/` should show the new type in `types.d.ts` and re-exported from `index.d.ts`.
2. `git diff types/python/relayport_types/` should show the new class in the appropriate file and re-exported from `__init__.py`.
3. `make typecheck` (which runs `tsc --noEmit` on `types/typescript/`) should pass.

If `make typecheck` fails, the most common cause is a removed/renamed export that the hand-maintained `types/typescript/index.d.ts` still references. Fix the barrel.
